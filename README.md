# 🧠 StudyMate AI

**StudyMate AI** is a lightweight, Python-based study assistant chatbot designed to help students quickly access resources for Mathematics, Physics, and Coding. Built by **ByteTalkers**, this application features a sleek dark-mode GUI and provides curated video links and PDF notes for various academic topics.

## 🚀 Features

* **Subject Specific Knowledge:** curated databases for:
    * **Math:** Calculus (Limits, Taylor Series) & Linear Algebra.
    * **Physics:** Mechanics (Kinematics, SHM) & Thermodynamics.
    * **Coding:** Python Basics, OOP, Data Structures & Algorithms.
* **Interactive GUI:** Built with `tkinter`, featuring a modern dark theme (`#0d1117`).
* **Smart Suggestions:** Dynamic "Quick Chips" buttons that update based on the selected subject.
* **Rich Media Support:** Clickable hyperlinks that open directly in your default web browser.
* **Offline Logic:** No API keys or internet connection required for the chatbot logic (only needed to view external links).

## 🛠️ Prerequisites

To run this application, you need **Python 3.x** installed on your system.

The application uses standard Python libraries, so **no external `pip` installations are required**. It relies on:
* `tkinter` (Standard GUI library)
* `webbrowser` (Standard URL handler)

## 📥 Installation & Usage

1.  **Clone or Download** the repository.
2.  Ensure you have the script file saved (e.g., `StudyMate_push_to_talk_FULL.py`).
3.  Open your terminal or command prompt.
4.  Navigate to the folder containing the file.
5.  Run the application:

```bash
python StudyMate_push_to_talk_FULL.py
